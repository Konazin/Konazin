from time import *
from tkinter import *
from tkinter import ttk

def tempo():
    localtime()
jan = Tk()
ttk.Label(jan, text= 'relogio').grid(column=0, row=0)
ttk.Label(jan, text= tempo).grid(column=1,row=1)
jan.geometry("500x500")
jan.mainloop()